{
  "name": "stocket-be",
  "version": "1.0.0",
  "description": "Backend multi-cliente de Stocket (Hono + Cloudflare D1). Proyecto nuevo e independiente — no toca inventario-be.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.13.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260821.1",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.7.3",
    "wrangler": "^4.125.0"
  }
}
