{
  "name": "stocket-be",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute stocket --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute stocket --remote --file=./schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.65.0"
  }
}
